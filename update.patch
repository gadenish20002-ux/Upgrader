--- components/upgrade-section.tsx
+++ components/upgrade-section.tsx
@@ -25,6 +25,7 @@
   const [targetId, setTargetId] = useState<string | null>(null)
   const [spinning, setSpinning] = useState(false)
   const [leftPanelMode, setLeftPanelMode] = useState<"inventory" | "shop">("inventory")
+  const [mobileTab, setMobileTab] = useState<"inventory" | "catalog">("inventory")
   const [isSettingsOpen, setIsSettingsOpen] = useState(false)
   const [balanceInput, setBalanceInput] = useState(0)
 
@@ -397,19 +398,34 @@
       {/* Bottom: Tabs (Mobile) / Grid (Desktop) */}
       <div className="w-full mt-4 lg:mt-3">
+        {/* Mobile Tabs */}
+        <div className="lg:hidden flex w-full bg-[#1C1D21] rounded-t-xl">
+          <button 
+            className={`flex-1 py-3.5 text-[0.875rem] font-semibold transition-colors ${mobileTab === "inventory" ? "bg-[#25262B] text-white rounded-t-xl" : "text-[#8A8E99]"}`}
+            onClick={() => setMobileTab("inventory")}
+          >
+            Мои предметы
+          </button>
+          <button 
+            className={`flex-1 py-3.5 text-[0.875rem] font-semibold transition-colors ${mobileTab === "catalog" ? "bg-[#25262B] text-white rounded-t-xl" : "text-[#8A8E99]"}`}
+            onClick={() => setMobileTab("catalog")}
+          >
+            Желаемые предметы
+          </button>
+        </div>
         {/* Content */}
-        <div className="grid w-full grid-cols-1 gap-6 lg:grid-cols-2 items-start mt-[-4px] lg:mt-0">
-          <div className="w-full">
+        <div className="grid w-full grid-cols-1 lg:grid-cols-2 items-start lg:gap-6">
+          <div className={`w-full ${mobileTab === "inventory" ? "block" : "hidden lg:block"}`}>
             <InventoryPanel 
               selectedUids={selectedUids} 
               onToggle={toggleItem} 
               mode={leftPanelMode}
               setMode={setLeftPanelMode}
               selectedShopIds={selectedShopIds}
               onToggleShopItem={toggleShopItem}
             />
           </div>
-          <div className="w-full">
+          <div className={`w-full ${mobileTab === "catalog" ? "block" : "hidden lg:block"}`}>
             <CatalogPanel 
               targetId={effectiveTarget} 
               onSelect={(id) => setTargetId(id)} 
